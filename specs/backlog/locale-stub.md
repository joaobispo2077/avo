# `/avo.locale` — design stub (blocked)

**Stop — do not implement** this command, skill, or engine job from this stub alone.

## Status

Design placeholder only. `/avo.locale` stays out of shipped slash commands until upstream
engine work for backlog items **BL-003**, **BL-004**, and **BL-005** is available and
documented in [`docs/ROADMAP.md`](../../docs/ROADMAP.md).

## Intended scope (future)

- Provider voice profile with consent (BL-003)
- Locale export pack from one English master (BL-004)
- Multi-audio delivery manifest per locale (BL-005)

## Agent rule

Do not scaffold `agent-skills/avo-locale`, `commands/avo/locale.md`, or pipeline wiring
for `/avo.locale` until this stub is replaced by an approved spec and the engine jobs ship.
